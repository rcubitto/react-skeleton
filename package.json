{
  "name": "landing",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "autoprefixer": "^9.4.2",
    "postcss-cli": "^6.0.1",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1",
    "tailwindcss": "^0.7.2"
  },
  "scripts": {
    "build:css": "postcss src/styles.css -o src/output.css",
    "watch:css": "postcss src/styles.css -o src/output.css -w",
    "start": "npm run watch:css & react-scripts start",
    "build": "npm run build:css && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
